Some notes so far:
- In clion, once you use the print function on any maze larger than 39 collumns wide, the print is not accurate anymore. This is because the line reaches its maximum length, and returns to a new line. BUT as far as I'm aware, everything is working just fine with the generate and print functions now.
